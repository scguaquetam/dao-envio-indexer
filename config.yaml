# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: dao-envio-indexer-dev
description: DAO governance indexer for dev (Rootstock Testnet)

# Performance optimizations
unordered_multichain_mode: true
preload_handlers: true
rollback_on_reorg: true

contracts:
  - name: Governor
    abi_file_path: ./abis/Governor.json
    handler: src/EventHandlers.ts
    events:
      # Proposal lifecycle events
      - event: 'ProposalCreated(uint256 proposalId, address proposer, address[] targets, uint256[] values, string[] signatures, bytes[] calldatas, uint256 voteStart, uint256 voteEnd, string description)'
      - event: 'ProposalCanceled(uint256 proposalId)'
      - event: 'ProposalExecuted(uint256 proposalId)'
      - event: 'ProposalQueued(uint256 proposalId, uint256 etaSeconds)'
      # Voting events
      - event: 'VoteCast(address indexed voter, uint256 proposalId, uint8 support, uint256 weight, string reason)'
      - event: 'VoteCastWithParams(address indexed voter, uint256 proposalId, uint8 support, uint256 weight, string reason, bytes params)'

networks:
  - id: 31  # Rootstock Testnet
    start_block: 5512037
    rpc_config:
      url: https://rpc.testnet.rootstock.io/SCrNhUpA96mAXUTYAu6kEofoHyRKTz-T
      # Rootstock RPC enforces a 2000-block limit on eth_getLogs.
      # These settings keep every query under that cap.
      initial_block_interval: 1999
      interval_ceiling: 1999
      backoff_multiplicative: 0.8
      acceleration_additive: 100
      backoff_millis: 5000
      query_timeout_millis: 20000
    contracts:
      - name: Governor
        address: 0xB1A39B8f57A55d1429324EEb1564122806eb297F
